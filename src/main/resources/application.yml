spring:
  application:
    name: eureka-server
server:
  port: 12345
eureka:
  instance: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance}:${server.port}/eureka